# Grafana Datasource Provisioning
# Auto-configures Loki for log exploration and dashboards.

apiVersion: 1

datasources:
  - name: Loki
    uid: loki-enterprise-rag
    type: loki
    access: proxy
    url: http://loki:3100
    editable: true
    jsonData:
      maxLines: 5000
